import styled from 'styled-components'
import { Section } from '../app/styled'

export const ContainerPageCategory = styled.div`
  text-align: center;
`
export const NewSectionCategory = styled(Section)`
  > a {
    width: 45%;
  }
  img {
    width: 100%;
    height: auto;
  }
`
